import React from 'react';
import TreeViewContainer from './treeViewContainer';

const Home = ({store}) => {
  return (
    <section className="treeView">
      <TreeViewContainer />
    </section>
  )
}

export default Home;
